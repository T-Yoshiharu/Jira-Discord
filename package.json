{
  "scripts": {
    "build": "tsc --noEmit --pretty false && esbuild src/main.ts --bundle --format=iife --platform=browser --target=es2019 --outfile=dist/main.js && prettier --write \"dist/**/*.js\" && node scripts/postprocess-dist.js",
    "push": "npm run build && clasp push"
  },
  "dependencies": {
    "@google/clasp": "^3.0.6-alpha",
    "@types/google-apps-script": "^1.0.99"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.25.11",
    "prettier": "^3.8.2",
    "typescript": "^5.9.3"
  }
}