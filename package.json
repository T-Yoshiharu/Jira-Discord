{
  "scripts": {
    "build": "tsc --pretty false && prettier --write \"dist/**/*.js\" && node scripts/postprocess-dist.js",
    "push": "npm run build && clasp push"
  },
  "dependencies": {
    "@google/clasp": "^3.0.6-alpha",
    "@types/google-apps-script": "^1.0.99"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "prettier": "^3.8.2",
    "typescript": "^5.9.3"
  }
}
